name: NZ→AU Premium Daily

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  nz-au-fares:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - env:
          BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
          RECIPIENT_EMAIL: "milo@imoffto.xyz"
          PE_PRICE_CAP: "650"
          CABIN_CLASS: "premiumeconomy"
          DRY_RUN: "0"
          LOG_LEVEL: "info"
        run: python3 nz_au_premium_daily.py

